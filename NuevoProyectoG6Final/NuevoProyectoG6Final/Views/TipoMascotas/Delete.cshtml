@model Vet.DAL.TipoMascota

@{
    ViewData["Title"] = "Delete";
}

<div class="card">
    <div class="card-header">
        <h2>¿Está seguro que desea eliminar este tipo de mascota?</h2>
    </div>

    <div class="card-body">
        <div>
            <h4>Tipo de Mascota</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Nombre)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Nombre)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="IdTipoMascota" />
                <input type="submit" value="Eliminar" class="btn btn-danger" /> |
                <a asp-action="Index" class="btn btn-outline-secondary">Atrás</a>
            </form>
        </div>
    </div>
</div>